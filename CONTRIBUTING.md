<div dir="rtl">

# المساهمة في بَصير

شكراً لاهتمامك. هذا الملف يختصر ما تحتاجه قبل أول PR.

---

## قبل أن تبدأ

افتح **issue** للنقاش قبل أي تغيير غير تافه. التغييرات المعمارية خصوصاً تحتاج
اتفاقاً مسبقاً — البنية هنا مقصودة والانحراف عنها يُرفض في المراجعة.

**الترخيص:** المشروع تحت [AGPL-3.0](./LICENSE). بمساهمتك توافق على نشر
مساهمتك تحت الترخيص نفسه.

---

## إعداد بيئة التطوير

```bash
python -m venv .venv && source .venv/bin/activate   # أو .\.venv\Scripts\Activate.ps1
pip install -r requirements-ci.txt                   # كافٍ للتطوير والاختبار
cp .env.example .env
```

للعمل على الاستدلال/التدريب فعلياً تحتاج `requirements.txt` كاملاً + PyTorch
بفهرس CUDA (راجع README).

على Linux، اختبارات الواجهة تحتاج مكتبات نظام:

```bash
sudo apt-get install -y ffmpeg libegl1 libgl1 libpulse0 libxkbcommon-x11-0
```

---

## قبل كل PR — شغّل هذه الأوامر

```bash
ruff check app tests scripts        # لينت
black app tests scripts             # تنسيق
python scripts/check_structure.py   # حدود حجم الملفات والدوالّ
mypy app                            # فحص الأنواع
QT_QPA_PLATFORM=offscreen pytest --cov=app --cov-fail-under=60
```

الخمسة كلها تعمل في CI. الـPR الذي يُفشل أياً منها لن يُدمَج.

---

## قواعد الكود

هذه ليست تفضيلات أسلوبية — كل واحدة نشأت من مشكلة حقيقية:

| القاعدة | السبب |
|---------|-------|
| `app/core/` **لا يستورد PyQt6** | يُبقي المنطق قابلاً للاختبار بلا شاشة ولا Qt |
| **لا SQL خام في `app/ui/`** | استعلام في طبقة العرض = استعلام غير مُختبَر. أضف تابعاً للخدمة |
| **لا وصول لـ`Service._db`** من الواجهة | نفس السبب؛ الخصوصية هنا ليست شكلية |
| **حقن التبعيات الثقيلة** (`inference_fn`, `FrameProvider`, `recognize_fn`) | ما يجعل 337 اختباراً يعمل في 20 ثانية بلا GPU |
| **Lazy imports** لـtorch/cv2/paddle/reportlab | إقلاع سريع وCI خفيف |
| **حدود حجم الملفات والدوالّ** | مفروضة في CI؛ الحدود في `docs/architecture.md` |
| **كل كتابة متعددة الخطوات داخل `db.transaction()`** | يمنع الحالات الوسطى عند الفشل |
| **الاستثناءات الواسعة تُبلَّغ للمستخدم** | `except Exception` + `logger` وحده يُخفي الأعطال؛ أعِد الإخفاق للواجهة |

---

## الواجهة تحديداً

- **الألوان من `app/ui/theme.py`** لا كـhex في الـview. أي تركيبة نص/خلفية
  جديدة يجب أن تتجاوز **4.5:1** (يفحصها `tests/test_review_fixes.py` آلياً).
- **كل زر له `setAccessibleName`** — خصوصاً الأزرار الرمزية (✓/✗/🎬).
- **لا تعتمد على اللون وحده** لنقل معلومة؛ أضف نصاً أو رمزاً.
- **RTL** مضبوط على مستوى التطبيق؛ استثنِ فقط ما هو إنجليزي بطبيعته (سجلات
  التدريب مثلاً `LeftToRight`).
- **العمليات الطويلة** عبر `app/workers/runner.run_worker` مع زر إيقاف مرئي.
- **اهرب من HTML** (`html.escape`) قبل حقن أي قيمة من المستخدم/نظام الملفات
  في نص `QLabel`.

---

## الاختبارات

- كل إصلاح خطأ يحتاج **اختباراً حارساً** يفشل قبل الإصلاح ويمر بعده. انظر
  `tests/test_review_fixes.py` كنموذج — كل اختبار هناك يوثّق سلوكاً كان خاطئاً.
- تغييرات المسار الكامل تحتاج تحديث `tests/test_e2e_pipeline.py`.
- لا تكتب اختبارات تحتاج GPU أو ملفات فيديو حقيقية — احقن بدائل.

---

## الفروع والـ PRs

```
main            ← الإصدار المستقر
claude/*        ← فروع التطوير (تُحذف بعد الدمج)
```

اكتب رسائل commit توضّح **لماذا** لا ماذا. الـPR يشرح المشكلة قبل الحل.

</div>
