# ============================================
# بَصير | Baseer — التبعيات
# ============================================

# AI / ML
# ملاحظة: torch/torchvision يُثبَّتان يدوياً بفهرس CUDA (راجع README) — التثبيت من
# PyPI هنا يسحب نسخة CPU. أبقيناهما مرجعاً للإصدار الأدنى.
ultralytics>=8.3.0
torch>=2.5.0
torchvision>=0.20.0
# opencv-python فقط — لا تُضِف opencv-contrib-python معه (يتصادمان على مساحة cv2)
opencv-python>=4.10.0
paddleocr>=2.8.0
paddlepaddle-gpu>=2.6.0
numpy>=1.26.0

# قاعدة البيانات
duckdb>=1.1.0
pandas>=2.2.0

# الواجهة
PyQt6>=6.7.0
PyQt6-Qt6>=6.7.0
pyqtgraph>=0.13.0

# معالجة الفيديو
ffmpeg-python>=0.2.0
scenedetect>=0.6.4
Pillow>=10.4.0
imagehash>=4.3.0

# النص العربي
arabic-reshaper>=3.0.0
python-bidi>=0.5.0

# التصدير
openpyxl>=3.1.0
reportlab>=4.2.0

# الإعدادات
python-dotenv>=1.0.0
pydantic>=2.8.0
pydantic-settings>=2.4.0

# أدوات التطوير
pytest>=8.3.0
pytest-qt>=4.4.0
pytest-cov>=5.0.0
black>=24.8.0
ruff>=0.6.0
mypy>=1.11.0
